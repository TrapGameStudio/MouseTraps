#pragma once
/// <summary>
/// Anchor is a point which defines the relative location of a shape
/// a anchorLocation point is on. 
/// <para>
/// This thing is hard to explain with words and my English sucks, so I may 
/// just add an example to show the difference in the future.
/// </para>
/// </summary>
enum Anchor {
    TopLeft,
    Top,
    TopRight,
    Left,
    Center,
    Right,
    BottomLeft,
    Bottom,
    BottomRight
};
